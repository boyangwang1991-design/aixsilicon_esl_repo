cmake_minimum_required(VERSION 3.19)
project(MeshConsumer LANGUAGES CXX)
if(MESH_SOURCE)
  add_subdirectory(${MESH_SOURCE} mesh-library)
else()
  find_package(AixEslNpuMesh 0.1 CONFIG REQUIRED)
endif()
add_executable(consumer main.cpp)
target_link_libraries(consumer PRIVATE aix::esl::npu_mesh)
