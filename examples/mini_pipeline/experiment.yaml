# mini_pipeline 参数扫描配置（R31 sweep 入口）
# 与 esl-testcase §10 的 E0/E1 对应：slot=1 与 slot=2。
schema_version: 1
system: examples/mini_pipeline/system.yaml
baseline:
  num_slots: 1
sweep:
  num_slots: [1, 2]
metrics:
  - total_ticks
  - data_ok
