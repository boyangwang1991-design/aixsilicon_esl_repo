# {{name}} model.yaml（compute 模板生成）
schema_version: 1
id: aixsilicon:esl:{{name}}:0.1.0
factory: {{name}}
model_kinds: [behavioral]
profiles:
  functional:
    timing_model: untimed
    data_modes: [full_data]
    interface_mode: command
    transport: direct
    capabilities: [data_transform]
ports: {command: command.consumer, done: event.producer}
parameters: {}
assumptions: []
