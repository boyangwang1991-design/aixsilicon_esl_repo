# 参考与历史原型

[legacy_python](legacy_python/README.md) 保留旧 Python 模型、时间调度、公共库、模板和测试，只此一份。它用于迁移比对与历史追踪，不是 SystemC 后端，也不是全部模型的独立算法 oracle。

新增纯算法参考按所属模型放 models/<name>/reference/python；没有算法需求就不建立 Python 镜像。
