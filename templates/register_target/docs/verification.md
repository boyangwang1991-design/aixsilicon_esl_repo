# Verification

examples/integration is the shared standalone, behavior and external-consumer fixture; no duplicate tests directory is needed. It checks two independent instances, read/write/initial values, annotated delay ownership, drain/reset/resume, address errors and DMI/debug refusal.

Template validation generates this asset in an isolated fixture and runs source, installed and relocated consumers. That evidence validates the template version, not later changes to a generated model. No hardware timing calibration or cycle accuracy is claimed.
